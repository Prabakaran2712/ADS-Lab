#include<bits/stdc++.h> 
using namespace std; 
class Bank{ public: int accno; string name; float balance; int no_of_transactions; int index; Bank create(Bank); void displayall(); Bank(){ index++; } static int find(int,vector<Bank>);  }; vector <Bank> accounts; Bank Bank::create( Bank temp){ cout<<"Enter the account number"; cin>>temp.accno; cout<<"Enter your name"; getchar(); getline(cin,temp.name); temp.balance=0; return temp; } int Bank::find(int accno,vector<Bank> accounts){ for(int i=0;i<accounts.size();i++){ if(accounts[i].accno==accno){ return i; } } return-1; } int main(){ int index=-1; int choice; int accno; while(1){ cout<<"Enter your choice : "; cin>>choice; if(choice==1){ Bank temp; temp=temp.create(temp); accounts.push_back(temp); } else if(choice==2){ for(int i=0;i<accounts.size();i++){ cout<<"accno "<<accounts[i].accno<<endl; cout<<"Name "<<accounts[i].name<<endl; cout<<"Balance "<<accounts[i].balance<<endl; accounts[i].index=++index; accounts[i].no_of_transactions=0; } } else if(choice==3){ cout<<"Enter the accno "; cin>>accno; int i=Bank::find(accno,accounts); if(i!=-1){ cout<<"accno "<<accounts[i].accno<<endl; cout<<"accno "<<accounts[i].name<<endl; cout<<"accno "<<accounts[i].balance<<endl;} else{ cout<<"Account Holder doesnot exist"; } } else if(choice == -1){ break; } else if(choice == 4){ float amnt; cout<<"Enter the accno "; cin>>accno; int i=Bank::find(accno,accounts); if(i!=-1){ cout<<"Enter the amount to deposit"; cin>>amnt; accounts[i].balance+=amnt; accounts[i].no_of_transactions+=1; } else{ cout<<"Account holder does not exist"; } } else if(choice == 5){ float amnt; cout<<"Enter the accno "; cin>>accno; int i=Bank::find(accno,accounts); if(i!=-1){ cout<<"Enter the amount to withdraw"; cin>>amnt; if( accounts[i].balance-amnt>=0){ accounts[i].balance-=amnt; accounts[i].no_of_transactions+=1; } else{ cout<<"Insufficient balance"; } } else{ cout<<"Account holder does not exist"; } } else if(choice==6){ cout<<"Enter the accno "; cin>>accno; int i=Bank::find(accno,accounts); cout<<"No of transactions "<<accounts[i].no_of_transactions; } else if(choice == 7){ cout<<"Enter the accno "; cin>>accno; int i=Bank::find(accno,accounts); accounts.erase(accounts.begin()+i); cout<<"Account Deleted "; } } ;  }
